<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Expense Tracker</title>

<style>
    body {
        margin:0;
        font-family: Arial, sans-serif;
        background-color: #f2f2f2;
    }


    #loginPage {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #222;
    }

    .login-box {
        background: white;
        padding: 25px;
        width: 350px;
        text-align: center;
        border-radius: 8px;
    }

    .login-box img {
        width: 100px;
        margin: 10px;
        border-radius: 6px;
    }

    .login-box input {
        width: 100%;
        padding: 8px;
        margin: 8px 0;
    }

    .login-box button {
        width: 100%;
        padding: 8px;
        background: #333;
        color: white;
        border: none;
        cursor: pointer;
    }

    #expensePage {
        display: none;
        background-image: url("https://t4.ftcdn.net/jpg/08/65/45/63/360_F_865456359_pcfzhNRwXSyPDfoOcOmH1vw4jFiLmGd1.jpg");
        background-size: cover;
        background-position: center;
        min-height: 100vh;
        padding-bottom: 20px;
    }

    header {
        background: rgba(0,0,0,0.7);
        color: white;
        text-align: center;
        padding: 30px;
    }

    .container {
        width: 90%;
        max-width: 900px;
        background-image: url("https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSzumMWXy2jvDLmpzocMOq1S1DRVV_cSdXyOw&s");
        background-size: cover;
        background-position: center;
        margin: 20px auto;
        padding: 20px;
        border-radius: 8px;
        background-color: rgba(255,255,255,0.9);
        background-blend-mode: overlay;
    }

    .image-row {
        display: flex;
        justify-content: space-around;
        flex-wrap: wrap;
        margin-bottom: 20px;
    }

    .image-row img {
        width: 130px;
        height: 90px;
        border-radius: 6px;
        margin: 5px;
    }

    .form-group {
        margin-bottom: 15px;
    }

    input, select, button {
        width: 100%;
        padding: 8px;
        margin-top: 5px;
    }

    button {
        background: #333;
        color: white;
        border: none;
        cursor: pointer;
    }

    .summary {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
    }

    .box {
        width: 32%;
        background: #eaeaea;
        padding: 10px;
        text-align: center;
        border-radius: 5px;
        font-weight: bold;
    }

    ul {
        list-style: none;
        padding: 0;
    }

    li {
        background: #f1f1f1;
        margin: 6px 0;
        padding: 8px;
        border-left: 5px solid black;
    }

    footer {
        background: rgba(0,0,0,0.7);
        color: white;
        text-align: center;
        padding: 15px;
    }
</style>
</head>

<body>


<div id="loginPage">
    <div class="login-box">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTxk61Ue8xeToUZI2KyVfPAgXWECiDq2KUHSA&s">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ7VmoyhG1NuJyu6TZoMMp09Y9oHakPE5IDqA&s">

        <h2>Login</h2>

        <input type="text" id="username" placeholder="Username">
        <input type="password" id="password" placeholder="Password">

        <button onclick="login()">Login</button>
    </div>
</div>


<div id="expensePage">

<header>
    <h1>Expense Tracker</h1>
</header>

<div class="container">

    <div class="image-row">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSN283odhZBhaMZDlkiYRvxu2VqhQSskysKBg&s">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ4HEOIPuJ8OLrDducdne0HiCvPH2zcSIonyw&s">
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQJ2hq5ouAf_erg0HWkUrWrb5FcxZrglw53Eg&s">
    </div>

    <h2>Add Expense</h2>

    <div class="form-group">
        <label>Expense Name</label>
        <input type="text" id="ename">
    </div>

    <div class="form-group">
        <label>Amount (₹)</label>
        <input type="number" id="amount">
    </div>

    <div class="form-group">
        <label>Type</label>
        <select id="type">
            <option value="expense">Expense</option>
            <option value="income">Income</option>
        </select>
    </div>

    <button onclick="addExpense()">Add Expense</button>

    <div class="summary">
        <div class="box">Income<br>₹ <span id="income">0</span></div>
        <div class="box">Expense<br>₹ <span id="expense">0</span></div>
        <div class="box">Balance<br>₹ <span id="balance">0</span></div>
    </div>

    <h2>Expense History</h2>
    <ul id="list"></ul>

</div>

<footer>
    <p>College Mini Project – Expense Tracker</p>
</footer>

</div>

<script>
    var totalIncome = 0;
    var totalExpense = 0;

    function login() {
        var user = document.getElementById("username").value;
        var pass = document.getElementById("password").value;

        if (user === "" || pass === "") {
            alert("Username and Password required");
            return;
        }

        if (pass.length < 6) {
            alert("Password must be at least 6 characters");
            return;
        }

        var hasNumber = false;
        var hasLetter = false;

        for (var i = 0; i < pass.length; i++) {
            if (pass[i] >= '0' && pass[i] <= '9') {
                hasNumber = true;
            }
            if ((pass[i] >= 'a' && pass[i] <= 'z') || (pass[i] >= 'A' && pass[i] <= 'Z')) {
                hasLetter = true;
            }
        }

        if (!hasNumber || !hasLetter) {
            alert("Password must contain letters and numbers");
            return;
        }

        document.getElementById("loginPage").style.display = "none";
        document.getElementById("expensePage").style.display = "block";
    }

    function addExpense() {
        var name = document.getElementById("ename").value;
        var amount = document.getElementById("amount").value;
        var type = document.getElementById("type").value;

        if (name.trim() === "" || amount === "") {
            alert("Please fill all fields");
            return;
        }

        amount = Number(amount);

        var li = document.createElement("li");
        li.innerText = name + " : ₹ " + amount;

        if (type === "income") {
            totalIncome += amount;
            li.style.borderLeft = "5px solid blue";
        } else {
            totalExpense += amount;
            li.style.borderLeft = "5px solid red";
        }

        document.getElementById("list").appendChild(li);

        document.getElementById("income").innerText = totalIncome;
        document.getElementById("expense").innerText = totalExpense;
        document.getElementById("balance").innerText = totalIncome - totalExpense;

        document.getElementById("ename").value = "";
        document.getElementById("amount").value = "";
    }
</script>

</body>
</html>
